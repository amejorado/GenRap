<%= simple_form_for(@concept) do |f| %>
  <%= f.error_notification %>

	<%= f.input :name %>
	<%= f.association :language, as: :select, :label => 'Lenguage', label_method: :name %>

  <div>
    <%= f.button :submit , {class: "btn btn-info"}%>
  </div>
<% end %>
