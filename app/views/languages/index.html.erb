<h1>Listado de lenguajes</h1>


<span style="display: inline-block;float:right;">
<%= link_to '<span class="glyphicon glyphicon-plus" style="margin-right: 5px;"></span>Nuevo Lenguaje'.html_safe, 
              new_language_path,
              :class => "btn btn-info" %>
</span>

<br/><br/>

<table class="table table-bordered table-striped">
  <tr>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @languages.each do |language| %>
  <tr>
    <td><%= language.name %></td>
    <td><%= link_to 'Ver más', language , {class: "btn btn-default btn-xs"}%></td>
    <td><%= link_to 'Editar', edit_language_path(language), {class: "btn btn-default btn-xs"} %></td>
    <td><%= link_to 'Borrar', language, method: :delete, data: { confirm: 'Estas seguro que deseas eliminar el lenguaje?' } , :class => "btn btn-default btn-xs" %></td>
  </tr>
<% end %>
</table>

<br />


