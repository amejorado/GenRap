<h1>Grupos</h1>
 
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Dueño</th>
      <th></th>
    </tr>
  </thead>
 
  <tbody>
  <% @groups.each do |group| %>
    <tr>
      <td><%= group.name %></td>
      <td><%= group.user.username %></td>
      <td><%= link_to 'Mostrar', group, {class: "btn btn-mini"}%>
      <% if check_admin %>
      <%=   link_to 'Editar', edit_group_path(group), {class: "btn btn-mini"} %>
      <%=   link_to 'Borrar', group, :confirm => '¿Está seguro que quiere borrar este grupo?', :method => :delete, :class => "btn btn-mini"  %>
      </td>
      <% end %>
    </tr>
  <% end %>
  </tbody>
</table>
 
<br />
 
<% if check_admin %>
  <%= link_to 'Nuevo Grupo', new_group_path %>
<% end %>